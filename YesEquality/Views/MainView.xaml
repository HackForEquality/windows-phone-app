<phone:PhoneApplicationPage
    x:Class="YesEquality.Views.MainView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
    xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
    xmlns:bab="clr-namespace:Caliburn.Micro.BindableAppBar;assembly=Caliburn.Micro.BindableAppBar"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    SupportedOrientations="Portrait" Orientation="Portrait"
    shell:SystemTray.IsVisible="True">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="480" />
            <RowDefinition Height="120" />
        </Grid.RowDefinitions>

        <!-- Viewfinder -->
        <Grid Grid.Row="1" Background="#dedede">
            
        </Grid>

        <!-- Camera button -->
        <Grid Grid.Row="2" VerticalAlignment="Center" HorizontalAlignment="Center">
            <Button x:Name="ShutterButton" Height="70" Width="70" Margin="0">
                <Button.Template>
                    <ControlTemplate>
                        <Image Source="/Resources/Assets/Buttons/Camera.png"/>
                    </ControlTemplate>
                </Button.Template>
            </Button>
        </Grid>
        
        <!-- AppBar -->
        <bab:BindableAppBar x:Name="Normal" BackgroundColor="White" ForegroundColor="Black">
            <bab:BindableAppBarButton x:Name="SwitchCamera" Text="Switch" IconUri="/Resources/Assets/Buttons/SwitchCamera.png"/>
            <bab:BindableAppBarButton x:Name="GoToBadges" Text="Badges" IconUri="/Resources/Assets/Buttons/Badges.png"/>
            <bab:BindableAppBarButton x:Name="GoToInfo" Text="Yes Equality" IconUri="/Resources/Assets/Buttons/Info.png"/>
            <bab:BindableAppBarMenuItem x:Name="GoToAbout" Text="About the app" />
        </bab:BindableAppBar>
    </Grid>

</phone:PhoneApplicationPage>